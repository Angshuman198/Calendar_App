name: main1
description: "A simple calendar app"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.25.0
  firebase_auth: ^4.17.0
  firebase_database: ^10.5.0
  cloud_firestore: ^4.13.6
  font_awesome_flutter: ^10.6.0  # Current
  google_sign_in: ^6.2.1         # Updated
  flutter_facebook_auth: ^6.0.3  # Updated  
  cupertino_icons: ^1.0.8        # Current

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
